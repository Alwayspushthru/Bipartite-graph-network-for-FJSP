# 方法说明文档：基于二部图注意力网络与 PPO 的柔性作业车间调度

> 本文档基于当前代码实现（`env/FJSPEnv.py`、`model/BiGraphNetwork.py`、`model/ppo.py`、`model/sub_layers.py`、`params.py`）整理，描述本项目求解 **柔性作业车间调度问题（FJSP）** 的完整方法。内容以源码为准，可能领先于 `README.md` 的部分描述。

---

## 目录

1. [问题定义与 MDP 建模](#1-问题定义与-mdp-建模)
2. [二部图状态表示与特征工程](#2-二部图状态表示与特征工程)
3. [二部图注意力网络（BiGraphNetwork）](#3-二部图注意力网络bigraphnetwork)
4. [训练：PPO 算法](#4-训练ppo-算法)
5. [推理与评估](#5-推理与评估)
6. [消融设计](#6-消融设计)
7. [关键超参数速查](#7-关键超参数速查)
8. [设计亮点小结](#8-设计亮点小结)

---

## 1. 问题定义与 MDP 建模

### 1.1 FJSP 问题

柔性作业车间调度问题（Flexible Job Shop Problem, FJSP）由 $J$ 个工件和 $M$ 台机器构成：

- 工件 $i$ 含一条有序工序链 $O_{i,1}\!\to\!O_{i,2}\!\to\!\cdots$，工序间有严格先后约束；
- 每道工序 $O$ 可在机器子集 $\mathcal{M}(O)\subseteq\{1,\dots,M\}$ 上加工，加工时间 $p_{O,m}$ 因机器而异；
- 需同时完成 **机器分配** 与 **工序排序** 两类决策，使最大完工时间 $C_{\max}$（makespan）最小。

### 1.2 序贯构造式 MDP

本项目以"逐工序构造解"的方式把调度建模为马尔可夫决策过程（MDP），用强化学习求解。每个决策步从所有合法的 (工件, 机器) 组合中选一个进行调度，直到所有工序完成。

| 要素                      | 定义                                                       |
| ------------------------- | ---------------------------------------------------------- |
| **状态 $s_t$**    | 部分解推导出的二部图（三类特征张量 + 动态可行性掩码）      |
| **动作 $a_t$**    | 整数$a\in[0,\,J\times M)$，解码为 (工件, 机器)           |
| **转移**            | 解析推进时间、推进候选指针、向后传播完工时间下界、重建特征 |
| **奖励 $r_t$**    | makespan 下界的改善量（稠密奖励）                          |
| **折扣 $\gamma$** | $1$（不打折，直接优化最终 makespan）                     |

**状态压缩.** 每步只暴露每个工件 **当前待调度的候选工序**，把状态规模从 $O(\#\text{ops}\times M)$ 压缩到 $O(J\times M)$。

**动作解码.**

```python
job     = action // n_machines
machine = action %  n_machines
```

不可行的 (工件, 机器) 对在网络层面被掩码（logits 置为 $-\infty$），保证采样动作恒合法。

**状态转移逻辑（`FJSPEnv.step`）.**

1. 解码动作 → 确定工件与机器；
2. 推进候选指针至下一道工序，若为末道则标记工件完成；
3. 解析计算时间：
   $$
   \text{start}=\max(\text{job\_ready},\,\text{machine\_ready}),\qquad \text{end}=\text{start}+p_{O,m};
   $$
4. 沿该工件工序链向后传播完工时间下界增量 $\Delta=\text{end}-\text{CT}_{\text{LB}}(O)$；
5. 重建工件 / 机器 / 边三类特征；
6. 全部工序完成时 `done = True`。

**奖励函数.** 基于 makespan 下界改善的稠密奖励：

$$
r_t = C_{\text{LB}}(s_t) - C_{\text{LB}}(s_{t+1}),\qquad C_{\text{LB}}=\max_O \text{CT}_{\text{LB}}(O).
$$

每步均有学习信号，避免稀疏奖励下的探索困难；整段 episode 的累积奖励近似等于总 makespan 改善量。

---

## 2. 二部图状态表示与特征工程

不同于 disjunctive graph，本方法用更紧凑的 **工件–机器二部图** 直接刻画资源分配关系。

### 2.1 归一化策略

- **加工时间归一化**：按实例 min–max 归一化到 $[0,1]$（仅对可加工项，`pt=0` 表示不可加工不参与）；
- **节点 / 边特征标准化**：z‑score 标准化，按实例统计，且工件特征仅对活跃节点统计。

### 2.2 三类特征张量

| 张量                  | 形状             | 含义                                     |
| --------------------- | ---------------- | ---------------------------------------- |
| `fea_j`             | `[B, J, 8]`    | 工件（候选工序）节点特征                 |
| `fea_m`             | `[B, M, 5]`    | 机器节点特征                             |
| `fea_pairs`         | `[B, J, M, 6]` | 工件–机器边特征                         |
| `dynamic_pair_mask` | `[B, J, M]`    | (工件, 机器) 对可行性掩码（True=不可行） |

**工件特征 `fea_j`（8 维，`construct_candidate_features`）**

| # | 特征                   | 含义                                                                     |
| - | ---------------------- | ------------------------------------------------------------------------ |
| 1 | `feasible_mas_ratio` | 当前工序可用机器数 / 总机器数                                            |
| 2 | `job_ready`          | 工件就绪时间（候选工序最早可开始时刻）                                   |
| 3 | `rem_ops`            | 剩余工序数 / 总工序数（调度进度）                                        |
| 4 | `rem_work`           | 剩余工序平均加工时间之和（剩余工作量估计）                               |
| 5 | `p_mean`             | 当前工序在所有可用机器上的平均加工时间                                   |
| 6 | `p_span`             | 当前工序最大−最小加工时间（机器间差异度）                               |
| 7 | `delay_ratio`        | 延迟比：候选相对其下界的拥塞程度，按柔性度加权                           |
| 8 | `criticality`        | 关键路径松弛：工件预计完工对全局 makespan 下界的接近度（1=在关键路径上） |

**机器特征 `fea_m`（5 维，`construct_mch_features`）**

| # | 特征                  | 含义                             |
| - | --------------------- | -------------------------------- |
| 1 | `feasible_ops_norm` | 可处理未调度工序数 / 活跃工件数  |
| 2 | `mach_ready`        | 机器下一次可用时间（空闲时刻）   |
| 3 | `expect_workload`   | 期望剩余负载（均匀责任分摊假设） |
| 4 | `idle`              | 机器累计空闲时间                 |
| 5 | `utilization`       | 机器利用率                       |

**边特征 `fea_pairs`（6 维，`construct_pair_features`）**

| # | 特征           | 含义                                            |
| - | -------------- | ----------------------------------------------- |
| 1 | `pt_out`     | 该对的加工时间（不可行对为 0）                  |
| 2 | `start`      | 最早开始时间 = max(工件就绪, 机器就绪)          |
| 3 | `wait_job`   | 工件等待机器时间 = max(0, 机器就绪 − 工件就绪) |
| 4 | `wait_mach`  | 机器等待工件时间 = max(0, 工件就绪 − 机器就绪) |
| 5 | `ratio_mach` | 加工时间 / 该机器候选工序平均加工时间           |
| 6 | `ratio_op`   | 加工时间 / 该工序在所有机器上的平均加工时间     |

> 这些特征同时编码 **静态加工属性** 与 **动态调度进度**，为注意力网络提供局部与全局兼具的决策依据。

---

## 3. 二部图注意力网络（BiGraphNetwork）

### 3.1 输入编码

三类原始特征分别经 2 层 MLP（GELU）嵌入到统一维度 $d=128$：

$$
h_j=\text{MLP}_j(\texttt{fea\_j}),\quad h_m=\text{MLP}_m(\texttt{fea\_m}),\quad h_{\text{pair}}=\text{MLP}_p(\texttt{fea\_pairs}).
$$

### 3.2 二部图注意力层（堆叠 2 层，`BiGraphLayer`）

每层执行三路更新。工件、机器节点的更新基于 **多头交叉注意力（默认 2 头）**，融合了两项 FJSP 专属机制：

**① 边特征注意力偏置（pair bias）** —— 边嵌入经 $W_a$ 投影成"每对、每头"的标量偏置，加到缩放点积分数：

$$
\text{score}=\frac{QK^\top}{\sqrt{d_h}}+W_a(h_{\text{pair}}).
$$

**② 边特征门控（gate）** —— 用注意力权重聚合的边上下文经 $W_g$ 与 $\tanh$ 生成乘性门，调制聚合消息：

$$
g=1+\tanh\!\big(W_g\,h_{\text{pair}}^{\text{ctx}}\big),\qquad \text{out}=\text{Attn}\odot g.
$$

**双向消息传递（并行，残差 + LayerNorm）：**

- 工件方向 $O\!\leftarrow\!M$：工件为 query，机器为 key/value；
- 机器方向 $M\!\leftarrow\!O$：边特征转置后，机器为 query。

$$
h_j\leftarrow\text{LN}(h_j+\text{Attn}_{O\leftarrow M}),\qquad h_m\leftarrow\text{LN}(h_m+\text{Attn}_{M\leftarrow O}).
$$

**边特征同步刷新**（保持节点与边表示一致）：

$$
h_{\text{pair}}\leftarrow\text{LN}\!\big(h_{\text{pair}}+\tanh(W_p[\,h_j\,\|\,h_m\,\|\,h_{\text{pair}}\,])\big).
$$

### 3.3 双路径投影

图编码后，嵌入被投影到两条用途不同的路径：

| 路径         | 维度              | 用途                                                    |
| ------------ | ----------------- | ------------------------------------------------------- |
| 局部高分辨率 | `actor_dim=32`  | 每个工件 / 机器 / 边的个体可辨识特征，供 Actor 逐对打分 |
| 全局紧凑     | `global_dim=16` | 经掩码均值池化得全局上下文，供 Critic 与时序记忆        |

### 3.4 跨步时序记忆（GRU）

将工件 / 机器 / 边三个全局均值拼成图级表示 $h_{\text{graph}}$，用 `GRUCell` 在 **决策步之间** 维护隐藏态 $h_{\text{hist}}\in\mathbb{R}^{64}$：

$$
h_{\text{hist}}^{(t)}=\text{GRU}\big(h_{\text{graph}}^{(t)},\,h_{\text{hist}}^{(t-1)}\big).
$$

这使策略从严格马尔可夫升级为携带调度历史的循环策略，捕捉构造解过程中跨步演化的趋势。

### 3.5 Actor 与 Critic 头

- **Actor**：把 [工件局部, 机器局部, 工件全局, 机器全局, 边局部, 时序记忆] 广播拼接为每个 (工件, 机器) 对的特征 `[B, J×M, ·]`，经 3 层 MLP 输出 logits，掩码后 Softmax 得策略 $\pi(a|s)$。
- **Critic**：以 [工件全局, 机器全局, 边全局, 时序记忆] 拼接为输入，经 3 层 MLP 输出标量价值 $V(s)$。

### 3.6 三种前向路径

| 方法                 | 用途                                                                |
| -------------------- | ------------------------------------------------------------------- |
| `forward`          | 单步前向，用于 rollout 与推理                                       |
| `forward_sequence` | 整段序列前向，PPO 更新时用当前参数重算$\pi/V$，并通过 GRU 做 BPTT |
| `act`              | 采样动作 + log‑prob + 价值（rollout 采集）                         |

---

## 4. 训练：PPO 算法

采用近端策略优化（PPO），Actor 与 Critic 共享骨干网络（`model/ppo.py`）。

### 4.1 优势估计（GAE）

- 使用 GAE（$\lambda=0.98$），对截断（非终止）rollout 用 $V(s_T)$ 自举，done 标志独立控制是否自举；
- **逐环境（沿时间维 $T$）标准化优势** —— 这是关键设计：奖励量纲随实例 makespan 缩放，逐环境标准化赋予策略梯度尺度不变性，对迁移到量纲差异巨大的基准数据集的泛化至关重要。
  > ⚠️ 切勿改为全局 $T\times B$ 标准化：会把梯度绑定到训练奖励尺度，严重损害 OOD 基准迁移（已被实验回退，见 `ppo.py` 注释 exp_log 20260603）。
  >

### 4.2 目标函数

$$
\mathcal{L}=c_p\,\mathcal{L}_{\text{clip}}+c_v\,\mathcal{L}_{V}+c_e\,\mathcal{L}_{\text{ent}},
$$

- $\mathcal{L}_{\text{clip}}$：裁剪的策略损失（$\epsilon=0.2$）；
- $\mathcal{L}_{V}$：价值 MSE 损失（价值目标用未标准化的原始优势）；
- $\mathcal{L}_{\text{ent}}$：熵正则（鼓励探索）；
- 梯度范数裁剪到 `max_norm=1.0`。

### 4.3 BPTT 与更新流程

每轮 PPO 更新（`k_epochs=4`）用 `forward_sequence` 在整段轨迹上以当前参数重算 $\pi$ 与 $V$，并通过 GRU 做 **真正的时序反向传播**；episode 终止处对隐藏态做 done 掩码重置。更新结束后将 `policy_old` 同步为最新参数。

### 4.4 在线数据生成

训练实例由 `SD3CaseGenerator`（`utils/data_utils.py`）在线随机采样，无需预先准备数据文件：

- **每作业工序数**：从 $[\lfloor 0.8M\rfloor,\ \lceil 1.2M\rceil]$ 随机取整；
- **柔性度**：每工序从 $[1, M]$ 随机取可加工机器数，再无重复抽取机器；
- **加工时间**：先取均值 `mean=randint(1,99)`，各兼容机器在均值 ±20% 内取整。

每隔 `reset_env_timestep` 步重采样，提升对不同规模与柔性度的泛化。

---

## 5. 推理与评估

训练好的策略支持多种解码方式（`params.py` 推理参数）：测试数据记录的是确定性集束方法。

| 模式       | 参数                     | 说明                                                                       |
| ---------- | ------------------------ | -------------------------------------------------------------------------- |
| 贪心       | `n_samples=1`          | argmax 确定性解码                                                          |
| 随机采样   | `n_samples>1`          | 多次随机 rollout 取最优 makespan                                           |
| 确定性集束 | `beam_width>1`         | 每步保留 K 条最可能的部分解                                                |
| 随机集束   | `beam_stochastic=True` | Gumbel‑top‑k（无放回采样 K 条），缓解高柔性/OOD 数据上确定性集束的"坍缩" |

集束推进时通过 `FJSPEnv.reorder(perm)` 按父代 beam 索引重排所有 **动态** 状态（静态实例数组保持不变）。

**评估基线**：公共测试集以权威论文中记录的（近）最优 makespan 为参照，报告相对最优解的 gap。

---

## 6. 消融设计

代码内置单开关消融（`--ablation`，每次运行一个变体，`none`=完整模型）：

| 变体             | 作用                                                     |
| ---------------- | -------------------------------------------------------- |
| `mean_agg`     | 以均匀均值聚合替代二部图注意力（去除 Q·K 分数与边偏置） |
| `no_pair_bias` | 去除边特征对注意力分数的调制                             |
| `no_gru`       | 屏蔽跨步时序记忆，退化为马尔可夫策略                     |

用于量化注意力机制、边特征调制、时序记忆三大核心组件各自的贡献。

---

## 7. 关键超参数速查

| 类别 | 参数                                  | 默认值         | 说明                              |
| ---- | ------------------------------------- | -------------- | --------------------------------- |
| 网络 | `fea_j/m/pair_input_dim`            | 8 / 5 / 6      | 三类原始特征维度                  |
| 网络 | 嵌入维度$d$                         | 128            | 节点 / 边统一嵌入                 |
| 网络 | `num_bigraph_layers`                | 2              | 二部图注意力层数                  |
| 网络 | 注意力头数                            | 2              | 多头交叉注意力                    |
| 网络 | `actor_dim / global_dim / hist_dim` | 32 / 16 / 64   | 局部 / 全局 / 时序记忆维度        |
| 网络 | `hidden_dim_actor/critic`           | 64             | Actor/Critic MLP 隐藏维度（3 层） |
| PPO  | `num_envs`                          | 20             | 并行训练环境数                    |
| PPO  | `max_updates`                       | 1000           | 训练更新轮数                      |
| PPO  | `lr`                                | 3e-4           | 学习率（Adam）                    |
| PPO  | `k_epochs`                          | 4              | 每轮 PPO 更新次数                 |
| PPO  | `eps_clip`                          | 0.2            | 裁剪参数                          |
| PPO  | `gamma`                             | 1.0            | 折扣因子（无折扣）                |
| PPO  | `gae_lambda`                        | 0.98           | GAE 平滑系数                      |
| PPO  | `vloss/ploss/entloss_coef`          | 0.5 / 1 / 0.01 | 损失权重                          |
| 训练 | `reset_env_timestep`                | 20             | 在线重采样数据间隔                |
| 训练 | `validate_timestep`                 | 10             | 验证 / 日志间隔                   |

---

## 8. 设计亮点小结

| 设计                                 | 说明                                                              |
| ------------------------------------ | ----------------------------------------------------------------- |
| **二部图结构**                 | 天然匹配 FJSP 工件–机器二部关系，比 disjunctive graph 更紧凑高效 |
| **多头注意力 + 边偏置 + 门控** | 注意力同时利用节点点积相似度与边特征调制，刻画分配的时间代价      |
| **节点与边同步更新**           | 双向消息传递后边特征随之刷新，保持结构表示一致                    |
| **双路径投影**                 | 局部高分辨率供精细打分，全局紧凑供价值与记忆，分工明确            |
| **GRU 时序记忆**               | 跨决策步携带调度历史，突破马尔可夫假设                            |
| **稠密奖励**                   | 基于 makespan 下界改善，每步有学习信号                            |
| **逐环境优势标准化**           | 尺度不变的策略梯度，是 OOD 泛化的关键                             |
| **在线数据生成**               | 每轮重采样实例，增强对规模 / 柔性度的泛化                         |
| **多样化推理**                 | 贪心 / 采样 / 确定性与随机集束，按数据分布灵活权衡质量与成本      |

---

*文档版本对应当前代码实现；如需 LaTeX 论文段落版或网络结构示意图，可在此基础上进一步生成。*
