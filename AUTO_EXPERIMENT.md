# AutoResearch 自动化实验作业手册

> 参考：[karpathy/autoresearch](https://github.com/karpathy/autoresearch)
> 定位：**通用方法论 + 标准作业流程（SOP）**。每次有一个小目标，按本手册的协议把它拆解为一组实验，由人或 Agent 执行。
> 适用范围：本仓库 [Bipartite-graph-network-for-FJSP](.) 的所有训练/调参/结构改造任务。

---

## 第一部分：方法论

### 1.1 核心理念

autoresearch 的本质是：**把"试错"工业化**。不再人工凭直觉调参，而是用一套机械可执行的协议，让一次实验变成"修改 → 训练 → 量化裁决 → git 保留/回滚"的闭环。

四条不可妥协的原则：

| 原则 | 含义 | 反例（违反时会发生什么） |
|------|------|------------------------|
| **唯一指标裁决** | 一次实验只看一个数字决定 keep / revert | 同时看 loss、gap、收敛速度 → 永远说不清"到底好不好" |
| **固定预算** | 同类实验使用同一资源预算（步数或时间） | 一个跑 200 步、另一个跑 1000 步 → 对比无意义 |
| **修改范围锁死** | 评估基准（环境、数据、指标计算）绝对不动 | 改了环境又改了模型 → 不知道收益来自哪 |
| **每步可回滚** | git 是安全网，差的结果一律 reset | 改坏了找不回上一个好版本 → 浪费整夜 |

### 1.2 适用与不适用

| ✅ 适合用本手册的任务 | ❌ 不适合的任务 |
|-------------------|----------------|
| 调超参（lr、clip、batch size 等） | 修 bug |
| 试结构变体（层数、维度、激活函数） | 重构代码 |
| 比较训练策略（GAE、entropy 系数等） | 加新功能（写新模块） |
| 验证某个 trick 是否有效 | 写文档、跑可视化 |

判断标准：**这件事能不能用"一个数字变好/变差"来评价？** 能 → 用本手册；不能 → 走常规开发流程。

---

## 第二部分：项目边界（一次性定义，长期不变）

### 2.1 禁区（评估基准，任何实验都不得修改）

```
env/FJSPEnv.py              ← 环境动力学，改一行所有历史结果作废
utils/                      ← 通用工具（种子、转换、日志）
data/                       ← 训练/验证/测试数据集
test.py 中的 makespan/gap 计算逻辑
results.tsv                 ← 历史记录，只追加不修改
```

**如果实验需要修改禁区**：先暂停所有实验 → 用单独的 commit 修改禁区 → 把这次修改作为新的"零号基准"，重新跑一遍当前最佳配置 → 之后的对比都基于新基准。

### 2.2 可改区（实验的搜索空间）

```
params.py                   ← 主战场，绝大多数实验只动这里
model/BiGraphNetwork.py     ← 结构改造时动
model/sub_layers.py         ← MLP/Actor/Critic 组件级改造
train.py                    ← 训练循环策略（如 GAE、loss 权重调度）
```

### 2.3 一次性配置：基准锚点

每次启动新一轮实验之前，确认当前的 baseline：

```bash
# 查看当前最佳配置（最近一次标记为 keep 的 commit）
git log --oneline --grep="\[keep\]" | head -1
```

如果是第一次跑，先用 `params.py` 默认值跑一次完整训练，把结果作为 `exp_000_baseline` 写入 `results.tsv`。

---

## 第三部分：实验单元的标准协议（SOP）

### 3.1 一次实验的生命周期

每次实验严格按 6 步走，缺一步都不算合规：

```
┌────────────────────────────────────────────────────────────┐
│  Step 1: 立项     - 在"实验单"中写清楚 hypothesis 与 change │
│  Step 2: 改码     - 只动可改区，且只动本次实验所需的部分    │
│  Step 3: 训练     - 用固定预算跑训练                       │
│  Step 4: 评估     - 在指定验证集上计算主指标                │
│  Step 5: 裁决     - 对比 baseline，决定 keep / revert       │
│  Step 6: 归档     - 写入 results.tsv + git commit/reset     │
└────────────────────────────────────────────────────────────┘
```

### 3.2 git 协议

**实验前**（确保工作区干净）：
```bash
git status                          # 必须是 clean，否则先 stash 或 commit
git log --oneline | head -3         # 确认当前 HEAD 是上一个 keep 状态
```

**实验后 - 情况 A：保留**（主指标改善超过阈值）
```bash
git add -A
git commit -m "[keep] exp#NNN: <主指标变化> | <一句话改动摘要>"
# 示例: "[keep] exp#012: gap 7.10→6.85 | hidden_dim 64→128"
```

**实验后 - 情况 B：丢弃**（主指标未改善或恶化）
```bash
git reset --hard HEAD
# 在 results.tsv 中仍要追加一行，status 标记 revert
```

**关键例外**：`results.tsv` 永远只追加不回滚。在 `git reset` 之前先把这次实验记录写入它，然后 commit 一次 results.tsv 的更新：
```bash
# 记录失败实验后
git add results.tsv
git commit -m "[log] exp#NNN: revert (gap 7.10→7.45)"
```

### 3.3 时间预算原则

不同阶段使用不同预算，原因是越早期的实验越要快速排除明显差的选项：

| 阶段 | 用途 | 预算 | 决策门槛 |
|------|------|------|---------|
| **筛选** | 在十几个候选中快速排除 | 短（如 200 updates） | 显著差就淘汰 |
| **标准** | 主要对比，决定 keep/revert | 中（如 500 updates） | 主指标改善 ≥ 阈值 |
| **确认** | 选出最优后做完整训练 | 长（如 1000+ updates） | 复现性验证 |

**同一阶段内必须使用同一预算**，跨阶段不可直接对比数值。

---

## 第四部分：从"小目标"到"实验计划"

每次拿到一个新小目标，按以下模板把它翻译成一组实验。

### 4.1 实验单模板（每个目标新建一份）

把这份模板复制到 `experiments/goal_<YYYYMMDD>_<short_name>.md`：

```markdown
# 目标: <一句话描述>

## 1. Hypothesis（假设）
   <预期改动会带来什么效果，为什么>

## 2. 主指标（Primary metric）
   - 名称: <如 val_gap% on SD2>
   - 当前 baseline 值: <数字>
   - 目标改善量: <绝对值或相对%>

## 3. 辅助指标（仅观察，不参与裁决）
   - <如 训练时长、显存峰值、推理延迟>

## 4. 搜索空间
   - 变量 A: [取值列表]
   - 变量 B: [取值列表]
   - 共 N 个实验

## 5. 预算
   - 每个实验: <步数或时间>
   - 总预算: <预估总时长>

## 6. 停止条件
   - 找到改善 ≥ 阈值的配置 → 进入"确认"阶段
   - 全部跑完无改善 → 写入 results.tsv 并归档，假设不成立
   - 预算耗尽 → 选当前最优继续

## 7. 实验日志
   | exp_id | 改动 | 主指标 | 决策 |
   |--------|------|-------|------|
   | NNN    | ...  | ...   | keep/revert |
```

### 4.2 如何选择搜索空间

按"改动最小、收益最可能大"的顺序，分三层：

1. **超参（最先试）**：lr、clip、entropy 系数等，改一个数字
2. **结构（次之）**：层数、维度、激活函数，改一处定义
3. **算法/策略（最后）**：换 GAE 实现、改 loss 形式、加正则化，改一段逻辑

经验法则：**一次实验只动一个维度**。如果同时改 lr 和层数，无法归因。

### 4.3 何时该停？

- ✅ **达到目标改善**：写入 results.tsv，本目标结束
- ✅ **三次连续无改善**：当前搜索方向可能错了，重新审视 hypothesis
- ❌ **不要无限刷指标**：第 N 次微调带来 0.05% 改善但加了 50 行代码 → 不值

---

## 第五部分：指标体系

### 5.1 主指标的选择

| 任务类型 | 推荐主指标 |
|---------|----------|
| 训练策略调整 | 验证集 makespan gap% |
| 结构改造 | 验证集 makespan gap%（同上，保持一致） |
| 推理加速 | 推理时延 + 主指标退化幅度（双约束） |
| 显存优化 | 显存峰值 + 主指标退化幅度（双约束） |

**核心约束**：一次目标只能有一个主指标。多目标 → 拆成多个子目标，每个子目标单独跑。

### 5.2 裁决规则

定义"显著改善"的阈值，避免噪声波动被当成进步：

```
显著改善 = |new - baseline| > max(noise_floor, relative_threshold * baseline)

其中：
- noise_floor: 主指标在同配置下的运行间方差（用 3 次重复跑估算）
- relative_threshold: 主指标的相对阈值（如 1%）
```

**重要**：每个新目标启动前，**先用 baseline 配置重复跑 3 次**，估算 `noise_floor`，写入实验单。否则你不知道 7.10 → 6.95 究竟是真改善还是噪声。

---

## 第六部分：记录系统

### 6.1 results.tsv（全局只追加日志）

位于项目根目录，所有实验都向它追加一行。字段：

```
exp_id       全局自增编号（如 042）
timestamp    实验完成时间
goal_id      所属目标的 short_name
hypothesis   一句话假设摘要
primary_metric_name   主指标名称
primary_metric_value  主指标值
baseline_value        当时 baseline 值
delta                 改善量（负数=更好，正数=更差）
changes      改动摘要（一句话）
budget       使用的预算（如 "500 updates"）
status       keep / revert / log_only
commit_sha   对应的 git commit（keep 时填写）
```

### 6.2 命名约定

- 实验 ID：`exp_NNN`，全局递增，永不复用
- 训练 run：`exp_NNN_<short_tag>`（例如 `exp_042_hidden128`）
- 模型权重：保存到 `trained_network/<data_source>/exp_NNN_<short_tag>.pth`
- TensorBoard 目录：`runs/exp_NNN_<short_tag>/`

### 6.3 目标归档

每个目标结束时，在对应的 `experiments/goal_*.md` 里补充：
- **结论**：假设是否成立
- **最优配置**：实验 ID 与参数
- **下游影响**：是否需要更新 baseline、是否产生新假设

---

## 第七部分：检查清单（合规自检）

### 实验前
- [ ] 工作区干净（`git status` 无未提交改动）
- [ ] 已写好实验单（`experiments/goal_*.md`）
- [ ] 已估算 baseline 的 noise floor
- [ ] 本次只动一个维度
- [ ] 本次只动可改区

### 实验中
- [ ] 使用了本阶段约定的固定预算
- [ ] 训练日志正常输出（无 NaN/崩溃）
- [ ] 没有顺手改其他东西

### 实验后
- [ ] 主指标已在指定验证集上计算
- [ ] 已在 results.tsv 追加一行
- [ ] keep 时：commit 信息符合 `[keep] exp#NNN: <metric> | <change>` 格式
- [ ] revert 时：results.tsv 的记录已先 commit，再 reset 代码改动
- [ ] 更新了实验单的"实验日志"表

---

## 第八部分：常见陷阱

| 陷阱 | 后果 | 规避方法 |
|------|------|---------|
| 同时改多个变量 | 无法归因 | 拆成多次实验 |
| 不同实验用了不同预算 | 对比无效 | 同阶段统一预算 |
| 没估算 noise floor 就裁决 | 把噪声当改善 | 先重复跑 3 次 baseline |
| 改了禁区文件 | 历史结果全部作废 | 严格遵守第 2.1 节 |
| 忘记 commit results.tsv 就 reset | 实验记录丢失 | 严格按 3.2 节流程 |
| 同一 exp_id 复用 | 日志冲突 | exp_id 全局递增，永不复用 |
| 无止境追求 0.01% 改善 | 时间黑洞 | 设定停止条件（4.3 节） |

---

## 附录 A：本项目可改区参数速查（示例搜索空间）

> 这是"工具箱"，不是"必做清单"。每个目标按需从中取用。

### PPO 超参（`params.py`）
| 参数 | 默认 | 常用搜索范围 |
|------|------|------------|
| `--lr` | `3e-4` | `1e-4`, `3e-4`, `1e-3` |
| `--eps_clip` | `0.2` | `0.1`, `0.2`, `0.3` |
| `--gae_lambda` | `0.98` | `0.90`, `0.95`, `0.98` |
| `--k_epochs` | `4` | `2`, `4`, `8` |
| `--entloss_coef` | `0.01` | `0.001`, `0.01`, `0.05` |
| `--vloss_coef` | `0.5` | `0.25`, `0.5`, `1.0` |

### 网络结构（`params.py` + `model/`）
| 参数 | 默认 | 常用搜索范围 |
|------|------|------------|
| `--num_bigraph_layers` | `2` | `1`, `2`, `3`, `4` |
| `--hidden_dim_actor` | `64` | `32`, `64`, `128`, `256` |
| `--hidden_dim_critic` | `64` | `32`, `64`, `128`, `256` |
| `--num_mlp_layers_actor` | `3` | `2`, `3`, `4` |
| `--num_mlp_layers_critic` | `3` | `2`, `3`, `4` |

### 训练策略
| 参数 | 默认 | 常用搜索范围 |
|------|------|------------|
| `--num_envs` | `20` | `10`, `20`, `40` |
| `--minibatch_size` | `1024` | `512`, `1024`, `2048` |
| `--reset_env_timestep` | `20` | `10`, `20`, `40` |

---

## 附录 B：与 autoresearch 原版的对应关系

| autoresearch 概念 | 本手册中的对应 |
|------------------|--------------|
| `train.py`（可改） | `params.py` + `model/` + `train.py` |
| `prepare.py`（禁区） | `env/` + `utils/` + `data/` + `test.py` 评估逻辑 |
| `program.md` | 本文档 |
| `val_bpb` | 各目标自选主指标（默认 makespan gap%） |
| 5 分钟 wall-clock | 固定 update 步数（按阶段：200/500/1000） |
| `results.tsv` | `results.tsv`（项目根目录） |
| 自主无限循环 | 每个目标有明确停止条件（不无限循环） |
